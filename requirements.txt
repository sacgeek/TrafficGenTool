# Controller dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
websockets>=12.0
python-multipart>=0.0.9

# Export (optional — falls back to text report if absent)
reportlab>=4.2.0

# Agent-only dependencies (install on worker VMs)
# websockets>=12.0          # already above
# playwright>=1.44.0        # run: playwright install chromium
# yt-dlp>=2024.5.1          # youtube worker
